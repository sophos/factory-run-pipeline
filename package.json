{
  "name": "action-run-pipeline",
  "version": "0.1.0",
  "scripts": {
    "build": "ncc build src/index.js --out dist/"
  },
  "dependencies": {
    "@actions/core": "^1.2.4",
    "@actions/github": "^4.0.0",
    "got": "^11.4.0"
  },
  "devDependencies": {
    "@zeit/ncc": "^0.22.3",
    "eslint": "^7.4.0",
    "prettier": "^2.0.5"
  }
}
